import React from "react";

const InlineButton = () => {
  const styles = {
    borderRadius: "10px",
    border: "none",
    padding: "10px",
    backgroundColor: "pink",
  };
  return <button style={styles}>Inline Button</button>;
};

export default InlineButton;
